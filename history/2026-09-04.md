# 2026-09-04

- Expanded `.gitignore` from 6 entries to a comprehensive Next.js-stack
  configuration (~40 entries): dependencies, build artifacts, Vercel,
  testing outputs (Playwright/Vitest), TypeScript build info, logs, editor
  configs, OS files.
- Security rationale: ignore `.env*` wholesale with a negated
  `!.env.example`, so production secrets (e.g. `SHEETS_SHARED_SECRET`)
  can never be committed by accident.
- Committed (`98a81cd`).
