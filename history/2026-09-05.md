# 2026-09-05

- Downloaded reference imagery from all three competitor sites and apple.com
  into `design-assets/reference/<site>/`, each folder with a `manifest.json`
  mapping files back to source URLs (~280 images total, ~48 MB).
  - `ionecare.in` is itself a Next.js site; originals were pulled from
    behind its `/_next/image` optimizer URLs.
  - apple.com assets organized per product: `iphone/`, `mac/`, `ipad/`,
    `watch/`, `airpods/`, `macbook-air/`, plus homepage hero tiles.
- Gitignored `design-assets/reference/**` (bulky, third-party copyright)
  while keeping the manifests and `design-assets/README.md` tracked.
- Researched apple.com's scroll-animation system by inspecting live markup,
  JS bundles, and CSS of the homepage, /macbook-air/, and /iphone/. Findings
  written to `docs/research/2026-09-05-apple-scroll-animations.md`:
  rAF scroll engine (not CSS scroll-timeline), viewport-relative keyframes
  in data attributes (`t - 200vh` load windows), section-scoped animation
  groups, muted playsinline videos with startframe posters as the animation
  medium, a single sparing `position:sticky`, transform-only animation with
  `prefers-reduced-motion` fallbacks — each mapped to a Motion (Framer)
  implementation for our stack.
- Started this `history/` work log (one dated file per work day) at the
  owner's request; backfilled 2026-09-03 and 2026-09-04 from git history.
- Owner curated the downloaded reference imagery: deleted `ionecare.in/`
  entirely and trimmed the rest to 112 keepers (apple.com 163→48,
  icareindia.in 37→34, icareservice.co.in 70→30). Each `manifest.json`
  regenerated to match what's on disk.
